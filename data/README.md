Place the UCI heart disease dataset here.

Expected default file:

```text
heart_disease_uci.csv
```

The configured target column is `num`. The analysis keeps the original severity
classes and trains a binary classifier where `num > 0` means heart disease risk.
