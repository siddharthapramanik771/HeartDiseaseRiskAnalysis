Training artifacts are written here.

Expected files after running training:

```text
model.joblib
training_metrics.json
```

Create them from the project root with:

```powershell
python -m src.train
```
